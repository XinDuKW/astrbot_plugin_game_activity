# 更新日志

## 1.0.1

图标、文档与测试脚手架的整理，**无功能变更**。

### 变更

- 新增插件图标 `logo.png`（AstrBot 会自动识别插件根目录下的该文件，无需在 `metadata.yaml` 中声明）。
- README 顶部加入口语化开场白并展示插件图标。
- README 常见问题新增「所有指令都完全没反应、日志也无报错」的排查入口；AGENTS 新增
  「改名 / 迁移的坑」章节，说明 AstrBot 的 `plugin_set`（配置 → 插件配置 → 可用插件）
  白名单在插件改名后会残留旧名字，导致该插件的指令**静默失效**。
- 修复测试脚手架：`tests/conftest.py` 之前只在插件目录名恰好等于包名时才能导入，
  从 GitHub 克隆或解压后（目录名通常是 `repo`）会出现 5 个测试收集错误。

## 1.0.0

首个版本。

### 命名

- 插件标识符为 `astrbot_plugin_game_event_due`（展示名「游戏活动日历」）。
  此前使用的 `astrbot_plugin_game_activity` 在发布审核系统中被判定为重复名，
  因此更换标识符；功能与配置未受影响。
- 更换标识符会同时更换 AstrBot 的插件数据目录与 WebUI 路由前缀：
  从旧标识符升级时请删除旧的插件目录，并重新执行 `/活动订阅`。

### 功能

- 聚合三个数据源，共支持 10 款游戏：
  - SRA 公共 API：崩坏：星穹铁道、原神、绝区零、鸣潮、异环、蔚蓝档案（国服 / 国际服 / 日服）
  - AKEData：明日方舟：终末地（含寻访卡池）
  - PRTS：明日方舟（含活动分类与兑换结束时间）
- 按会话（群聊 / 私聊）独立订阅，支持一个会话订阅多款游戏。
- 新活动提醒：活动第一次出现在会话视野中时推送一次，支持提前预告窗口。
- 倒计时提醒：活动结束前剩余 7 天 / 3 天 / 24 小时各提醒一次，档位可按会话自定义或关闭。
- 每会话关键词黑白名单过滤，匹配活动名、描述与标签。
- 推送消息默认**直接发送活动封面图片**，文字与图片交替排版。
- **封面自动压缩**：下载后等比缩放到 720px 并重编码为 JPEG，体积降到原体积的约 5%~10%
  （终末地 12 张 1.6 MB → 116 KB，星铁 7 张 6.7 MB → 502 KB）。
- **单条消息封面总体积上限**（默认 5 MB），超出预算的封面自动降级为文字链接而非丢弃；
  含图消息整体发送失败时再降级一次（全部换成链接）重试。
- **图片 / 链接可按会话切换**：全局配置决定默认值，`/活动封面 图片|链接|关闭|默认`
  或管理页可让每个群/私聊单独覆盖。
- **相邻活动之间插入分隔线**（`activity_separator`，默认 `━━━━━━━━━━━━`），一条消息推送
  多个活动时边界更清楚；紧跟分段标题的第一个活动不加线，留空则只用空行分隔。
- 同一游戏同一轮的提醒合并成一条消息；跨年活动自动补全年份。
- 只有成功送达的提醒才写入状态，发送失败自动在下一轮重试。
- 聊天指令：订阅 / 退订 / 列表 / 查询 / 游戏列表 / 过滤 / 开关 / 阈值 / 状态 / 刷新。
- Plugin Pages 管理页：查看与编辑所有会话的订阅、档位与过滤，预览当前活动，手动触发检查。

### 实现要点

- 活动数据源统一抽象为 `ActivitySource`，新增游戏只需实现一个数据源。
- 时间统一使用东八区固定偏移，避免 Windows 缺少 tzdata 时 `zoneinfo` 失效。
- HTTP 层带 ETag 与磁盘缓存，数据源不可用时回退到上次成功结果并在状态里提示。
- AKEData 只在游戏大版本变化时下载表数据（hotfix 不触发），且不下载 10 MB 的
  `CharacterTable.json`；文本表按大版本共享缓存，旧版本缓存自动清理。
- PRTS 使用 Semantic MediaWiki 的 `action=ask` 一次取回全部活动，并强制最小请求间隔
  以规避站点 WAF。
- 提醒状态按活动结束时间自动裁剪，不会无限增长。
